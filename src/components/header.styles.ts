import styled from 'styled-components';

export const StyledHeader = styled.div`
  padding: 0 10px;
  height: 48px;

  & > div:first-child {
    height: 100%;
  }

  & > div:last-child {
    margin: 0;
  }
`;
